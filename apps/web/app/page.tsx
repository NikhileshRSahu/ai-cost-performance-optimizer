export default function HomePage() {
  return (
    <section className="hero" aria-labelledby="page-title">
      <p className="eyebrow">Founder workbench</p>
      <h1 id="page-title">Reduce AI spend with evidence, not guesswork.</h1>
      <p className="lede">
        Observe cost, detect a defensible opportunity, benchmark the candidate,
        and verify the real net impact after implementation.
      </p>
      <div className="evidence-note">
        This workbench separates potential, tested, and verified savings. A
        cheaper candidate is never treated as safe until your configured
        performance requirements pass.
      </div>
    </section>
  );
}
